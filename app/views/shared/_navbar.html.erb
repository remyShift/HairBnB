<nav class="bg-slate-200 w-full h-[10vh] min-h-16 flex justify-between items-center px-5 shadow-md"
    data-controller="toggle-login">
    <%= link_to "HairBnB", root_path, class: "text-2xl md:text-3xl font-bold font-kaushan text-primary" %>

    <% if user_signed_in? %>
        <div class="flex items-center gap-3">
            <p class="text-sm text-gray-600">
                Welcome, <span class="font-bold text-xl text-primary font-kaushan"><%= current_user.first_name %></span>
            </p>
            <i class="fa-solid fa-user-large text-xl cursor-pointer text-primary" data-action="click->toggle-login#toggle"></i>
        </div>
    <% else %>
        <i class="fa-solid fa-user-large text-xl cursor-pointer" data-action="click->toggle-login#toggle"></i>
    <% end %>

    <div class="bg-slate-100 w-44 h-auto border border-slate-600 absolute flex-col justify-center top-20 right-4 rounded-md transition-opacity duration-300 opacity-0 px-4 hidden ease-in-out"
        data-toggle-login-target="loginSignup">
        <% if user_signed_in? %>
            <%= link_to "Mon profil", edit_user_registration_path, class: "text-sm py-2 cursor-pointer text-center hover:text-primary transition-colors duration-300" %>
            <div class="w-full h-[1px] bg-slate-300 rounded-full my-2"></div>
            <%= link_to "Déconnexion", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-sm py-2 cursor-pointer text-center hover:text-primary transition-colors duration-300" %>
        <% else %>
            <p class="text-sm py-2 cursor-pointer text-center hover:text-primary transition-colors duration-300"
                data-action="click->toggle-login#toggleOverlayLogin">Login</p>
            <div class="w-full h-[1px] bg-slate-300 rounded-full my-2"></div>
            <p class="text-sm py-2 cursor-pointer text-center hover:text-primary transition-colors duration-300"
                data-action="click->toggle-login#toggleOverlaySignup">Sign Up</p>
        <% end %>
    </div>

    <%= render "shared/modal" %>
</nav>

<div class="w-full h-[1px] bg-slate-600">
</div>
